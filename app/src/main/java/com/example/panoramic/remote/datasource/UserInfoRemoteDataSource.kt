package com.example.panoramic.remote.datasource

import com.example.panoramic.remote.model.PlaceDto
import com.example.panoramic.remote.service.UserInfoService

class UserInfoRemoteDataSource (private val userInfoService: UserInfoService) {

   // fun getUserInfo(): List<PlaceDto>? {
   //     return userInfoService.getUserInfoResponse()
  //          .response?.items
   // }
}